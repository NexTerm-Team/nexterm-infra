apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexterm-web.fullname" . }}
  labels:
    {{- include "nexterm-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "nexterm-web.selectorLabels" . | nindent 4 }}
